import { CanActivateStepTwo } from './can-activate-step-two';

describe('CanActivateStepTwo', () => {
  it('should create an instance', () => {
    expect(new CanActivateStepTwo()).toBeTruthy();
  });
});
